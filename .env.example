# Optional direct calendar OAuth apps. Register your own installed desktop applications.
# Tokens and provider keys entered in Settings are stored in this app's encrypted store.
GOOGLE_CALENDAR_CLIENT_ID=
GOOGLE_CALENDAR_CLIENT_SECRET=
MICROSOFT_CALENDAR_CLIENT_ID=

# Optional local development settings. No hosted OpenWhispr configuration is used.
UI_LANGUAGE=en
LOQUI_CACHE_ROOT=
