# Diagnostics

Use the pinned Node and Node SQLite binding for tests. After renderer build and Electron rebuild, `node scripts/smoke-electron.cjs` performs isolated account-free/offline desktop checks. Linux CI uses xvfb-run. Review temporary logs/screenshots for private content before sharing. Hardware/signed-release checks are listed in docs/VERIFICATION.md.
