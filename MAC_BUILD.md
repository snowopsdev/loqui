# Apple Silicon development build

Use Apple Silicon, Node from .node-version, CMake and Xcode tools with SDK 26 or newer. Follow README preparation commands, then `npm run build:mac`. Development packages are ad-hoc signed; public packages must use the protected signing/notarization workflow in docs/RELEASING.md.
