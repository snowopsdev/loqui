# Network destinations

See [privacy documentation](PRIVACY.md). Loqui has no hosted backend or OpenWhispr service requests. GitHub release checks/downloads are optional after setup. User-selected providers, Codex, model downloads, calendars and URL imports make their own requests. Runtime build downloads are listed exactly in runtime-assets.json.
