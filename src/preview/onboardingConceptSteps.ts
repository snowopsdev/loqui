export type ConceptStep = "speech" | "cleanup" | "try" | "shortcuts" | "finish";

export const CONCEPT_STEPS: readonly ConceptStep[] = [
  "speech",
  "cleanup",
  "try",
  "shortcuts",
  "finish",
];
